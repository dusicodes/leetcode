# Personal questions 

Pattern check
- Why is this a sliding window / DP / graph problem?
"it wasn't a sliding window it was most a sorting algor that requires 2 pointers"
- What signals told me that?
"the question itself prettl clare"

Logic check
- What was the key insight that unlocked it?
"i need to learn sorting algorithms lmao "
- Could I explain it without code?
"yea"

Failure check
- What confused me initially?
"nothing really confused me it was quite straigtforward"
"i didn't read the question properly so firstly i thought that i just needed to find if the word was in ther haystack"
- What did I try first that was wrong?
"everything at first was fine i passed most of the test cases"
"i did't think about the constrains tho"
"which made me fail some cases"

Correct lOgic 
"based on solution it is to be used the dutch flag algorithm" 
"the problem with his is that it for 3 distict values"

Given an array arr[] consisting of only 0s, 1s, and 2s. The objective is to sort the array, i.e., put all 0s first, then all 1s and all 2s in last.

This problem is the same as the famous "Dutch National Flag problem". The problem was proposed by Edsger Dijkstra. The problem is as follows:

Given n balls of colour red, white or blue arranged in a line in random order. You have to arrange all the balls such that the balls with the same colours are adjacent with the order of the balls, with the order of the colours being red, white and blue (i.e., all red coloured balls come first then the white coloured balls and then the blue coloured balls).
