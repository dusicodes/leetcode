# Personal questions 

Pattern check
- Why is this a  hashmap / DP / graph problem?
    we are required to compare duplicates of a string to find anagrams
- What signals told me that?
    the string is a permutation of another string, so we must detect if they have the same letters in the string

Logic check
- What was the key insight that unlocked it?

- Could I explain it without code?
    yes 

Failure check
- What confused me initially?
    is how to dtermine if two strings are permutations of each other
    all i did is sort the words indivusually and check if they are equal
    then create a hash map and check if the values are the same

- What did I try first that was wrong?


